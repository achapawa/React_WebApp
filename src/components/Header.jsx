import React from "react";
import Navbar from "./Navbar";

function Header(){
    return <header>
        <Navbar />
    </header> //remember <header> is a html element so starts with lowercase letter.
}


export default Header;
